service nais
{
# Our service is not in /etc/hosts
    type         = UNLISTED
    protocol     = tcp
    socket_type  = stream
    port         = 6666
    server       = /opt/problem/nais 
    user         = nais
    wait         = no
}
